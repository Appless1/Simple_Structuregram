pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "simple-structuregram"

// dependencyResolutionManagement removed to prevent "Unresolved reference" errors.
// Repositories are successfully handled in build.gradle.kts instead.